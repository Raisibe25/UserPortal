spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_portal
    username: user_portal_user
    password: StrongPassword123!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true